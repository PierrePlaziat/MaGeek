<UserControl x:Class="MaGeek.UI.DeckTable"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MaGeek.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#222">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="26"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <DockPanel>
        </DockPanel>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="25"></ColumnDefinition>
                <ColumnDefinition Width="25"></ColumnDefinition>
                <ColumnDefinition Width="25"></ColumnDefinition>
                <ColumnDefinition Width="25"></ColumnDefinition>
                <ColumnDefinition Width="25"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label FontWeight="Bold" Foreground="White" Content="{Binding CurrentDeck.Title}"></Label>
            <Label Grid.Column="1" FontWeight="Bold" Foreground="Black" Content="{Binding CurrentDeck.DevotionB}"></Label>
            <Label Grid.Column="2" FontWeight="Bold" Foreground="White" Content="{Binding CurrentDeck.DevotionW}"></Label>
            <Label Grid.Column="3" FontWeight="Bold" Foreground="DodgerBlue" Content="{Binding CurrentDeck.DevotionU}"></Label>
            <Label Grid.Column="4" FontWeight="Bold" Foreground="DarkSeaGreen" Content="{Binding CurrentDeck.DevotionG}"></Label>
            <Label Grid.Column="5" FontWeight="Bold" Foreground="IndianRed" Content="{Binding CurrentDeck.DevotionR}"></Label>

        </Grid>

        <!--<Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Alphabetic"></Button>
            <Button Grid.Column="1" Content="Cmc"></Button>
            <Button Grid.Column="2" Content="Type"></Button>

        </Grid>-->


        <ListView Name="LVDeck" ItemsSource="{Binding CurrentDeck.CardRelations}" 
                   BorderThickness="0"   Background="#222" Grid.Row="2" 
                  SelectionChanged="LVDeck_SelectionChanged">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Background="Black" Height="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"></ColumnDefinition>
                            <ColumnDefinition Width="11"></ColumnDefinition>
                            <ColumnDefinition Width="20"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <Label Foreground="White" Padding="4,1,1,1" Grid.Column="3" Content="{Binding Card.CardForeignName}"></Label>

                        <local:ManaCost Grid.Column="4" Cost="{Binding Card.ManaCost}" HorizontalAlignment="Right"/>

                        <Button Grid.Column="0" Height="20" Foreground="Black" Content="-" Click="LessCard"></Button>
                        <Label  Grid.Column="1" Height="20" Background="#555" Padding="2" Foreground="White" Content="{Binding Quantity}"></Label>
                        <Button Grid.Column="2" Height="20" Foreground="Black" Content="+" Click="MoreCard"></Button>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="0" 
                    Grid.Column="1" 
                    Grid.RowSpan="3" 
              Background="#111"
                    HorizontalAlignment="Stretch"/>

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Stretch"
              Grid.Column="1" 
              Grid.Row="0"  
              Grid.RowSpan="3"
              Background="#111">
            <WrapPanel Name="UGrid" HorizontalAlignment="Stretch" MouseWheel="UGrid_MouseWheel"/>
        </ScrollViewer>

        <StackPanel Grid.Column="1" 
                    Grid.Row="0"  
                    Grid.RowSpan="3" 
                    Background="#111"
                    Visibility="{Binding Loading}">
            <Label>Loading...</Label>
        </StackPanel>

        <StackPanel Visibility="{Binding Visible}" Grid.RowSpan="3" Grid.ColumnSpan="2" Background="#444">
            <Label>No deck selected</Label>
        </StackPanel>

    </Grid>
    
</UserControl>
